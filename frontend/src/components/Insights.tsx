import Card from '@/components/Card'

/**
 * Will render the `insights` string list from POST /analyze as a
 * readable findings list in a later phase.
 */
function Insights() {
  return <Card eyebrow="Findings" title="Insights" />
}

export default Insights
